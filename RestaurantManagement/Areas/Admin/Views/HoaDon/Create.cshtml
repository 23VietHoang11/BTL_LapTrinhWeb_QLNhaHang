@* @model RestaurantManagement.Areas.Admin.Models.ViewModels.HoaDonCreateEditViewModel
@{
    ViewData["Title"] = "Thêm Hóa đơn mới";
}

<h2>Thêm Hóa đơn mới</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="IdkhachHang" class="control-label"></label>
                <input asp-for="IdkhachHang" class="form-control" placeholder="ID Khách hàng (để trống nếu khách vãng lai)" />
                <span asp-validation-for="IdkhachHang" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="TrangThai" class="control-label"></label>
                <select asp-for="TrangThai" class="form-control">
                    <option value="Mới">Mới</option>
                    <option value="Đã thanh toán">Đã thanh toán</option>
                    <option value="Đã hủy">Đã hủy</option>
                </select>
                <span asp-validation-for="TrangThai" class="text-danger"></span>
            </div>

            <div class="form-group" style="margin-top: 20px;">
                <input type="submit" value="Tạo và Thêm Chi tiết" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@