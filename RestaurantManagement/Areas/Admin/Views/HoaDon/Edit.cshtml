@* @model RestaurantManagement.Areas.Admin.Models.ViewModels.HoaDonCreateEditViewModel
@{
    ViewData["Title"] = "Sửa Hóa đơn: " + Model.IdhoaDon;
}

<h2>Sửa Hóa đơn</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdhoaDon" />

            <div class="form-group">
                <label class="control-label">Ngày Lập:</label>
                <input value="@Model.NgayLap.ToString("dd/MM/yyyy HH:mm")" class="form-control" readonly />
            </div>

            <div class="form-group">
                <label class="control-label">Tổng Tiền:</label>
                <input value="@Model.TongTien.ToString("N0")" class="form-control" readonly />
            </div>

            <div class="form-group">
                <label asp-for="IdkhachHang" class="control-label"></label>
                <input asp-for="IdkhachHang" class="form-control" placeholder="ID Khách hàng" />
                <span asp-validation-for="IdkhachHang" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="TrangThai" class="control-label"></label>
                <select asp-for="TrangThai" class="form-control">
                    <option value="Mới">Mới</option>
                    <option value="Đã thanh toán" selected="@(Model.TrangThai == "Đã thanh toán" ? "selected" : null)">Đã thanh toán</option>
                    <option value="Đã hủy" selected="@(Model.TrangThai == "Đã hủy" ? "selected" : null)">Đã hủy</option>
                </select>
                <span asp-validation-for="TrangThai" class="text-danger"></span>
            </div>

            <div class="form-group" style="margin-top: 20px;">
                <input type="submit" value="Cập Nhật" class="btn btn-primary" />
                <a asp-action="Details" asp-route-id="@Model.IdhoaDon" class="btn btn-info">Chi tiết</a>
                <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@